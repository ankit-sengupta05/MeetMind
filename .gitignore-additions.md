# =============================================================================
# MeetMind – .gitignore additions (monorepo-specific)
# The root .gitignore already handles node_modules, .env, dist, etc.
# These entries cover workspace-specific artifacts
# =============================================================================

# Compiled output
**/dist/
**/*.tsbuildinfo
**/*.js.map

# Test coverage
**/coverage/

# Azure Functions local config (contains connection strings)
functions/local.settings.json

# Vite build cache
client/.vite/

# Teams app package (built artifact, not source)
*.zip
meetmind-teams-app.zip
teams-package/

# Azure deployment artifacts
.deployment
deploy.cmd

# Editor / OS
.DS_Store
Thumbs.db
*.suo
*.user
.idea/
.vs/
